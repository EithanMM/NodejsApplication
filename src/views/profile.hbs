<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="card-body">
                    <h3>Welcome {{user.FullName}}</h3>
                    <h4>{{user.UserName}}</h4>
                    <a href="/links" class="btn btn-success m-4">Save your Contacts!</a>
                </div>
            </div>
        </div>
    </div>
</div>